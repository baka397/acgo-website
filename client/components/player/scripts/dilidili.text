(function getPlayer(){
    let frame=document.querySelector('.player_main');
    let content=frame.children[0];
    content.setAttribute('width','100%');
    content.setAttribute('height','100%');
    document.write(frame.innerHTML);
})();